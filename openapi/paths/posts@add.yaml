post:
  tags:
    - Posts
  description: |
    Registers new post
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/posts/RegisterPost.yaml
  responses:
    "200":
      content:
        application/json:
          schema:
            $ref: ../components/schemas/posts/Post.yaml