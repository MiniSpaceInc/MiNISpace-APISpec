post:
  tags:
    - OrganizingUnits
  summary: Add organizing unit
  description: Adds organizing unit (optionally with parent id of parent Unit)
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/organizingUnits/RegisterOrganizingUnit.yaml
  responses:
    "200":
      description: "OK"
      content:
        application/json:
          schema:
            $ref: ../components/schemas/organizingUnits/OrganizingUnit.yaml
