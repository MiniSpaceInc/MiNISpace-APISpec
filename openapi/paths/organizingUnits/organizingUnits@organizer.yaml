get:
  tags:
    - OrganizingUnits
  summary: Get organizing units of a user
  description: Gets all organizing units a user belongs to determined by userId embedded in user's acces token.
  responses:
    "200":
      description: "OK"
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../../components/schemas/organizingUnits/OrganizingUnit.yaml
